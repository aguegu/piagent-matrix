{
  "name": "piagent-matrix",
  "version": "0.2.0",
  "private": true,
  "description": "Talk to the pi coding agent from any Matrix client — end-to-end encrypted, with a persistent crypto store.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test --test-concurrency=1 \"test/**/*.test.js\"",
    "cross-sign": "node scripts/cross-sign.js"
  },
  "license": "MIT",
  "dependencies": {
    "@earendil-works/pi-coding-agent": "^0.84.3",
    "config": "^5.0.1",
    "dotenv-flow": "^4.1.0",
    "markdown-it": "^15.0.0",
    "matrix-bot-sdk": "^0.8.0",
    "sanitize-html": "^2.17.7"
  },
  "devDependencies": {
    "matrix-js-sdk": "^42.2.0"
  }
}
